import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand Colors
  static Color primary = Color(0xFF00228E);
  // static Color secondary = Color(0xFF006E54);
  // static Color accent = Color(0xFF89A7FF);
}
